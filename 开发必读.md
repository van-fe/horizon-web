# 开发必读
汇总了组件开发相关的内容，欢迎补充~

## 功能设计
开发一个组件之前，肯定会思考这个组件要哪些功能？要定义哪些 props 和 emits？如何跟设计稿进行结合？  
很多人会去参考 Element、AntD，除此之外非常建议再看看一些国外流行的组件库，如 [Vuetify](https://vuetifyjs.com/en/components/alerts/)、[Quasar](https://quasar.dev/vue-components/) 等，可能会有不一样的启发。  
举个例子：对于 `Link` 组件，Element 只提供了 `href` 进行跳转，而 Vuetify 除了提供 `href` 之外，还有 `to`, `replace` 这样的 prop 结合 `vue-router` 进行路由跳转，这显然要更加实用。

另外，需要注意的是，不要复制粘贴 Element 等开源库的代码和文档，文档中的话术可以参考设计稿中的描述进行整合。

## 开发相关
### 运行环境
为兼容 M1 Mac，请使用 node.js >= 16 的版本。建议使用 [nvm](https://github.com/nvm-sh/nvm) 下载及切换版本。  

分支：请基于 feature 分支自行创建开发分支。

bun：目前支持1.0版本及以上

### 生成模板
使用 `bun run new component <componentName>` 即可自动生成一个组件目录和预置代码，帮忙你快速进入开发。示例：
```bash
bun run new component PanelItem
```

使用 `bun run new directive <directiveName>` 即可自动生成一个指令目录和预置代码，帮忙你快速进入开发。注意指令名称必须以 `v-` 开头。示例：
```bash
bun run new directive v-focus
```

### 开发前准备
在最外层根目录下，执行：
```shell
bun run build
```

### 启动开发服务
在最外层根目录下，执行：
```shell
bun run dev
```

### 目录结构
```
├── node_modules  # packages 中所有项目公共的模块
└── packages  # bun 的工作空间（Workspace），包含了多个项目
    ├── horizon-web      # horizon-web UI 项目
    │   ├── dist    # 编译生成的文件目录
    │   ├── lib     # 编译生成的 esm 文件目录
    │   └── src
    │       ├── components      # 组件目录
    │       ├── directives      # 指令目录
    │       ├── composables     # 公共的类 Hook 文件目录
    │       └── styles          # 样式目录
    ├── doc     # 文档项目
    │   ├── docs      # 包含所有 markdown 文件的目录
    │   │   ├── components      # 组件文档目录
    │   │   │   └── Button      # 具体某组件的文档目录
    │   │   │       ├── demo        # 存放 .vue 示例文件的目录
    │   │   │       └── doc.md      # markdown 文档
    │   │   ├── directives                # 指令文档目录
    │   │   ├── methods                   # 方法文档目录
    │   │   ├── getting-started           # 快速开始文档目录
    │   │   ├── styles-and-animations     # 样式和动画文档目录
    │   │   └── ...                       # 其他文档目录
    │   ├── src  # 文档项目自身的源码目录
    ├── icon                # 图标库
    ├── locale              # 多语言库（纯js，无框架代码）
    ├── locale-vue          # 多语言库，提供 vue 支持
    ├── shared              # 工具库
    └── unplugin-resolver   # 用于提供给 unplugin-vue-components 做树摇的 resolver
```

### useProps
组件的 props 必须定义在 `./src/composables/useProps.ts` 文件中，且为了能正确生成文档，需要遵循这些规则：
* 组件参数的注释对应文档中的参数说明
  ```ts
  export const useXXXProps = {
    /** switch 关闭时的自定义状态文本 */
    statusOffText: {
      type: String,
      default: 'Off',
    },
  };
  ```
* 注释中支持 Markdown 语法，但为了保证良好的阅读体验和排版，请尽量只使用如加粗、斜体、链接等有限语法。
  ```ts
  export const useXXXProps = {
    /**
     * 哈哈哈
     * **绑定**的值啊，测试下[链接](#/components/switch)功能
     */
    modelValue: {
      type: Boolean,
      default: false,
      required: true,
    },
  };
  ```
* 不要写 `enum`，使用 `UnionType` 替代。
  ```ts
  export const useXXXProps = {
    /** switch 大小 */
    size: {
      type: String as PropType<'normal' | 'small'>,
      default: 'normal',
    },
  };
  ```
* 尽量总是设置 `required` 属性，文档中的 `是否必填` 会将这个属性作为最高优先级判断。
* `PropType` 的泛型类型，暂时只支持基本类型、`UnionType` 和 `Interface`，按组件库的实际情况看应该可以满足大多数需求，如果你确实需要其他类型请联系我。

### 导出关联组件
一个组件内可能会包含它的关联组件，如 `Checkbox` 组件可能还内置了 `CheckboxGroup`，此时由于编译生成的文件只有 `Checkbox.js` 而没有 `CheckboxGroup.js`，所以自动按需加载会失败，请手动修改 `packages/unplugin-resolver/src/index.mts` 中的 config。

### 编码规范

#### style 样式文件
只能使用 scss

* scss 等文件中不支持路径别名
  ```scss
  // not support
  @import '~/styles/name.scss';
  @import '@/styles/name.scss';
  ```

## 文档相关
所有文档都在 `packages/doc/docs` 目录下。

组件以及指令的文档必须是 `packages/doc/docs/[components|directives]/[组件名]/doc.md`，文件名 `doc.md` 请不要修改。

文档标准结构，右侧菜单，demo等内容根据文档结构生成，请注意遵循

```
### 特性1

描述特性1

::: demo1 :::

### 特性2

描述特性2

::: demo2 :::
```

文档中的 demo 使用如下语法，`demo` 后的路径表示相对于当前位置的组件路径。
```
:::demo ./demos/basic.vue :::
```

在某些场景下你可能希望仅展示 demo 效果（Vue 渲染的结果），而不展示源码，例如颜色面板，给文件名添加 `--no-code` 即可：
```
:::demo ./demos/colors--no-code.vue :::
```

组件/指令/方法 的文档不需要去定义除demo以外的信息，全都会自动解析。

## 依赖库
当前组件库依赖了如下第三方库，你可以在需要时利用这些库提供的能力：

### [@vueuse/core](https://vueuse.org/)
提供了基于 Composition API 的一系列工具方法，如 `useEventListener`, `useMouse`, `useLocalStorage` 等。

### [async-validator](https://github.com/yiminghe/async-validator)
一个异步验证表单的库。

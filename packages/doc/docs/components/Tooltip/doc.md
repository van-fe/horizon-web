### 基础 
通过设置 `placement` 来改变 `tooltip` 出现位置
:::demo ./demos/basic.vue :::

### 显示位置
通过设置 `placement` 来改变 `tooltip` 出现位置
:::demo ./demos/placement.vue :::

### 宽度设置
最小宽度是 `64px` ，最大宽度是 `320px`
:::demo ./demos/width.vue :::

### 是否允许移入后复制
设置 `enterable = true`，即可移入复制
:::demo ./demos/enterable.vue :::

<!-- ### 尺寸
自 `2.0.5` 开始， `tooltip` 支持 `small` 尺寸
:::demo ./demos/size.vue ::: -->

<!-- ### 主题
自 `2.7.0` 开始， `tooltip` 支持 `light` 主题
:::demo ./demos/theme.vue ::: -->

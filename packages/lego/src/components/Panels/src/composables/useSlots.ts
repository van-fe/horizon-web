export const usePanelsSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type PanelsSlots = typeof usePanelsSlots;

export const usePanelSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type PanelSlots = typeof usePanelSlots;

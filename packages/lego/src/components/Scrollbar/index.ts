import { default as Scrollbar } from './src/Scrollbar';
import { withInstall } from '@nio-fe/shared';
import './src/utils/declare';

export const NScrollbar = withInstall(Scrollbar);
export default NScrollbar;

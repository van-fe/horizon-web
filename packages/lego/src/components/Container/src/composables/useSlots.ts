export const useContainerSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type ContainerSlots = typeof useContainerSlots;

export const useAsideSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type AsideSlots = typeof useAsideSlots;

export const useFooterSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type FooterSlots = typeof useFooterSlots;

export const useHeaderSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type HeaderSlots = typeof useHeaderSlots;

export const useMainSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type MainSlots = typeof useMainSlots;

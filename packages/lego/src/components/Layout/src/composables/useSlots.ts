export const useRowSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type RowSlots = typeof useRowSlots;

export const useColumnSlots = {
  /**
   * 默认展示的内容
   */
  default: () => true,
};

export type ColumnSlots = typeof useColumnSlots;

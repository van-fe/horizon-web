## Basic usage
Type a trigger character to filter candidates. The suggestion panel follows the text caret across wrapped lines and textarea scrolling. Select with arrow keys, Enter, or Tab; use `placement` to set the preferred side, with automatic flipping when space is limited.
:::demo components/Mentions/basic.vue :::

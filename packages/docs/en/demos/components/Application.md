## i18n Configuration
Configure multiple languages through Application, allowing your application to switch languages at any time.
:::demo components/Application/i18n.vue :::

## size Configuration
Through the `size` props, you can control the size of all components

However, not all components support `small` | `large`, please pay attention to the component documentation when using
:::demo components/Application/size.vue :::

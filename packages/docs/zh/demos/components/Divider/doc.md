### 基础用法
:::demo ./demos/basic.vue :::

### 垂直分割线
:::demo ./demos/vertical.vue :::

### 线的样式
通过配置 `line-style`，可以改变线的类型
:::demo ./demos/line-style.vue :::

### 插入文字
使用 `slots.default` ，可以调配文字插槽。配合 `props.titlePlacement`，控制文字位置
:::demo ./demos/slots.vue :::

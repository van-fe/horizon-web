### 基础用法
:::demo ./demos/basic.vue :::

### 标签位置
可以通过配置 `label-position` 控制标签的位置
:::demo ./demos/labelPosition.vue :::

### 尺寸
提供了 `medium/small` 两种尺寸
:::demo ./demos/size.vue :::

### 禁用和只读
使用 `disabled` 和 `readonly` 控制是否禁用和只读
:::demo ./demos/disabled.vue :::

### 拦截修改
配置 `before-change`，可以拦截是否允许改变值
:::demo ./demos/beforeChange.vue :::

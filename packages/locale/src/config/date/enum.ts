export enum DateLocaleAvailableShownType {
  SHORT = 'short',
  LONG = 'long',
  SHORT_TIME = 'short_time',
  LONG_TIME = 'long_time',
}

export enum DateLocaleAvailableDateType {
  YEAR = 'year',
  MONTH = 'month',
  DAY = 'day',
  HOUR = 'hour',
  MINUTE = 'minute',
  SECOND = 'second',
  WEEKDAY = 'weekday',
  TIME_ZONE_NAME = 'timeZoneName',
  HOUR12 = 'hour12',
}

export enum DateLocaleAvailableFormattingType {
  NUMERIC = 'numeric',
  '2-DIGIT' = '2-digit',
  LONG = 'long',
  SHORT = 'short',
  NARROW = 'narrow',
}
